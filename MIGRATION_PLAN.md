# ПЛАН МІГРАЦІЇ ТОВАРІВ ЗІ СТАРОГО САЙТУ НА НОВИЙ

**Проект:** BeautyShop  
**Старий сайт:** https://beautyshop-ukrane.com.ua/ (OpenCart)  
**Новий сайт:** Django проект  
**Дата створення плану:** 06.11.2025

---

## 📋 ЗАГАЛЬНИЙ ОГЛЯД ЗАВДАННЯ

### Мета
Повна міграція товарів, категорій, підкатегорій, фільтрів, цінової градації та оптових цін зі старого OpenCart сайту на новий Django проект без дублювань та з повним збереженням інформації.

### Масштаб роботи
- **Категорії:** аналіз повної ієрархії
- **Підкатегорії:** створення моделі підкатегорій в Django
- **Товари:** перевірка та міграція всіх товарів
- **Фільтри:** створення системи фільтрів для кожної категорії
- **Ціни:** градація цін (3+ шт, 5+ шт), оптові ціни, роздрібні ціни
- **Зображення:** міграція всіх фото товарів
- **Характеристики:** міграція всіх атрибутів товарів

---

## 🎯 ЕТАП 1: АНАЛІЗ СТАРОГО САЙТУ

### 1.1 Аналіз структури категорій
**Завдання:** Скласти повний список категорій та їх ієрархії

**Що перевірити:**
- [ ] Увійти в адмін-панель старого сайту
- [ ] Перейти в розділ "Категорії" (`catalog/category`)
- [ ] Експортувати/скопіювати повний список категорій
- [ ] Визначити батьківські категорії (верхній рівень)
- [ ] Визначити дочірні категорії (підкатегорії)
- [ ] Перевірити вкладеність (чи є багаторівнева ієрархія)
- [ ] Зафіксувати назви категорій українською
- [ ] Зафіксувати описи категорій (якщо є)
- [ ] Зафіксувати зображення категорій (якщо є)
- [ ] Перевірити URL/slug категорій
- [ ] Визначити порядок сортування (sort_order)
- [ ] Визначити активні/неактивні категорії

**Результат:**
- Excel/Google таблиця з такими колонками:
  - ID (старий)
  - Назва
  - Батьківська категорія
  - Slug
  - Опис
  - Шлях до зображення
  - Порядок сортування
  - Статус (активна/неактивна)

---

### 1.2 Аналіз підкатегорій (субкатегорій)
**Завдання:** Визначити всі підкатегорії та їх зв'язки з категоріями

**Що перевірити:**
- [ ] Перевірити чи використовується поле "parent" в категоріях OpenCart
- [ ] Визначити всі підкатегорії для кожної категорії
- [ ] Перевірити чи є підкатегорії 2-го рівня (підкатегорії підкатегорій)
- [ ] Зафіксувати повну ієрархію: Категорія → Підкатегорія → Підпідкатегорія
- [ ] Визначити яка логіка фільтрації використовується зараз
- [ ] Перевірити чи є окремі "групи товарів" або "бренди" що використовуються як фільтри

**Питання для з'ясування:**
- Чи використовуються підкатегорії на старому сайті як фільтри?
- Чи відображаються підкатегорії на окремих сторінках чи тільки як фільтри?
- Яка максимальна глибина вкладеності категорій?

**Результат:**
- Діаграма ієрархії категорій (дерево)
- Таблиця з повним списком підкатегорій та їх батьків

---

### 1.3 Аналіз товарів
**Завдання:** Перевірити всі товари та їх дані

**Що перевірити:**
- [ ] Відкрити розділ "Товари" (`catalog/product`)
- [ ] Визначити загальну кількість товарів
- [ ] Перевірити які товари вже мігровані (порівняти з поточною БД Django)
- [ ] Визначити які товари не мігровані
- [ ] Перевірити структуру даних товару:
  - Назва товару
  - Артикул (SKU/Model)
  - Категорія (основна)
  - Додаткові категорії (якщо є)
  - Опис (короткий та повний)
  - Ціна (базова)
  - Спеціальна ціна (special price)
  - Градація цін (quantity discounts/tier prices)
  - Знижки для груп клієнтів (customer group prices)
  - Кількість на складі
  - Статус (активний/неактивний)
  - Зображення (головне та додаткові)
  - Характеристики/атрибути
  - SEO дані (meta title, meta description)
  - Теги/ключові слова

**Критичні поля для міграції:**
1. Базова інформація (назва, опис, артикул)
2. Ціни (всі типи)
3. Зображення (усі фото)
4. Характеристики
5. Зв'язки з категоріями
6. Статус товару

**Результат:**
- Список всіх товарів (ID, назва, категорія)
- Список товарів що не мігровані
- Таблиця з порівнянням полів OpenCart vs Django

---

### 1.4 Аналіз цінової системи
**Завдання:** Повністю зрозуміти ціноутворення на старому сайті

**Що перевірити:**
- [ ] Базова ціна (price)
- [ ] Спеціальна ціна (special price) - аналог sale_price
- [ ] Градація цін за кількістю (Tier Pricing / Quantity Discounts):
  - Від 3 штук - яка ціна?
  - Від 5 штук - яка ціна?
  - Від 10 штук - яка ціна? (якщо є)
- [ ] Ціни для груп клієнтів (Customer Group Prices):
  - Роздрібні клієнти (по замовчуванню)
  - Оптові клієнти (wholesale)
  - Інші групи (якщо є)
- [ ] Відсотки знижок
- [ ] Акційні ціни з датами (start date, end date)

**Критичні питання:**
- Яка градація цін використовується найчастіше? (3шт, 5шт, 10шт?)
- Чи застосовуються знижки автоматично при досягненні кількості?
- Чи є різниця між оптовими цінами та градацією цін?
- Як відображаються ціни для незареєстрованих користувачів?
- Як відображаються ціни для зареєстрованих користувачів?

**Результат:**
- Документ з описом всієї системи ціноутворення
- Приклади 5-10 товарів з різними типами цін
- Таблиця відповідності OpenCart → Django:
  - `price` → `retail_price`
  - `special` → `sale_price`
  - `tier_price (qty 3)` → `price_3_qty`
  - `tier_price (qty 5)` → `price_5_qty`
  - `customer_group (wholesale)` → `wholesale_price`

---

### 1.5 Аналіз фільтрів
**Завдання:** Визначити які фільтри використовуються на старому сайті

**Що перевірити:**
- [ ] Перейти в розділ "Фільтри" (`catalog/filter`)
- [ ] Визначити які фільтри існують
- [ ] Перевірити до яких категорій прив'язані фільтри
- [ ] Визначити типи фільтрів:
  - За брендом
  - За призначенням
  - За групою товарів
  - За ціною
  - За наявністю
  - За характеристиками (об'єм, колір, тип)
- [ ] Перевірити структуру даних фільтра:
  - Назва фільтра
  - Група фільтра
  - Значення фільтра
  - Категорії де застосовується

**Приклад аналізу для категорії "Волосся":**
- Бренд: [Biki, Estel, Kapous, ...]
- Призначення: [Шампунь, Маска, Бальзам, ...]
- Об'єм: [250мл, 500мл, 1000мл, ...]
- Тип волосся: [Сухе, Жирне, Нормальне, ...]

**Результат:**
- Таблиця фільтрів:
  - Категорія
  - Назва фільтра
  - Тип фільтра
  - Значення (варіанти)
- Схема прив'язки фільтрів до категорій

---

### 1.6 Аналіз характеристик товарів
**Завдання:** Визначити які характеристики/атрибути використовуються

**Що перевірити:**
- [ ] Перейти в розділ "Характеристики" (`catalog/attribute`)
- [ ] Визначити список всіх характеристик
- [ ] Згрупувати характеристики за типом:
  - Об'єм/вага
  - Склад/інгредієнти
  - Бренд/виробник
  - Країна виробництва
  - Тип/категорія
  - Колір
  - Запах
  - Інше
- [ ] Перевірити як характеристики прив'язані до товарів
- [ ] Визначити які характеристики використовуються для фільтрації

**Результат:**
- Список всіх характеристик з описом
- Таблиця відповідності характеристик → фільтрів

---

### 1.7 Аналіз зображень
**Завдання:** Визначити структуру зберігання зображень

**Що перевірити:**
- [ ] Шлях до зображень на старому сайті
- [ ] Формат зображень (jpg, png, webp)
- [ ] Розміри зображень
- [ ] Кількість зображень на товар (головне + додаткові)
- [ ] Наявність watermark/логотипів
- [ ] Якість зображень

**Критичні питання:**
- Чи потрібно завантажувати зображення зі старого сайту?
- Чи є оригінальні зображення у високій якості?
- Чи потрібна оптимізація зображень перед завантаженням?
- Використовувати Cloudinary чи локальне зберігання спочатку?

**Результат:**
- План міграції зображень
- Скрипт для завантаження зображень (якщо потрібно)

---

## 🏗️ ЕТАП 2: ПІДГОТОВКА DJANGO ПРОЕКТУ

### 2.1 Аналіз поточної структури моделей
**Завдання:** Перевірити чи достатньо поточних моделей для міграції

**Що перевірити:**
- [x] Модель `Category` (є, підтримує ієрархію через `parent`)
- [x] Модель `Product` (є, підтримує різні типи цін)
- [ ] Модель `Subcategory` - **ПОТРІБНО СТВОРИТИ?**
- [ ] Модель для фільтрів - **ЧАСТКОВО Є** (`ProductGroup`, `ProductPurpose`, `Brand`)
- [x] Модель для характеристик (є - `ProductAttribute`)
- [x] Модель для зображень (є - `ProductImage`)
- [x] Модель для тегів (є - `ProductTag`)

**Поточна структура цін в Django Product:**
```python
retail_price          # Роздрібна ціна
wholesale_price       # Оптова ціна (для зареєстрованих)
sale_price           # Акційна ціна
price_3_qty          # Ціна від 3 шт
price_5_qty          # Ціна від 5 шт
```

**Що працює добре:**
- ✅ Система градації цін повністю реалізована
- ✅ Підтримка оптових цін
- ✅ Підтримка акційних цін з датами
- ✅ Ієрархія категорій через `parent`
- ✅ Система тегів для фільтрації

**Що потрібно доопрацювати:**
- ⚠️ Розкоментувати поля `brand`, `product_group`, `purpose` в моделі Product
- ⚠️ Створити міграцію для цих полів
- ⚠️ Логіка підкатегорій (використовувати `parent` в Category чи створити окрему модель?)
- ⚠️ Конфігурація фільтрів для категорій (є `CategoryFilterConfig` але потрібно налаштувати)

**Результат:**
- Список змін що потрібно внести в моделі
- План міграцій бази даних

---

### 2.2 Рішення щодо підкатегорій
**Завдання:** Визначити як реалізувати підкатегорії

**Варіант 1: Використовувати поле `parent` в Category**
✅ Переваги:
- Вже реалізовано
- Нескінченна вкладеність
- Простота структури
- Метод `get_all_children()` вже є

❌ Недоліки:
- Може бути складно розділити логіку категорій та підкатегорій в UI

**Варіант 2: Створити окрему модель Subcategory**
✅ Переваги:
- Чітке розділення категорій та підкатегорій
- Легше налаштувати окрему логіку фільтрації
- Простіше для розуміння

❌ Недоліки:
- Додаткова модель
- Дублювання коду
- Складніше при багаторівневій ієрархії

**РЕКОМЕНДАЦІЯ:** Використовувати Варіант 1 (поле `parent`)
**Обґрунтування:**
- Модель вже є та працює
- Більша гнучкість
- Відповідає Django best practices
- Менше коду для підтримки

**Логіка відображення на фронтенді:**
```
При відкритті категорії:
1. Показати список підкатегорій (children) як перший блок фільтрів
2. Дозволити вибрати одну або кілька підкатегорій
3. Нижче показати інші фільтри (бренд, ціна тощо)
4. Фільтрація працює кумулятивно (підкатегорії + фільтри)
```

**Результат:**
- Затверджене архітектурне рішення
- План реалізації UI для підкатегорій

---

### 2.3 Планування системи фільтрів
**Завдання:** Спроектувати повну систему фільтрації

**Поточні моделі для фільтрів:**
```python
Brand              # Бренди (закоментовано в Product)
ProductGroup       # Групи товарів (закоментовано в Product)  
ProductPurpose     # Призначення (закоментовано в Product)
ProductTag         # Теги (активно, many-to-many)
CategoryFilterConfig  # Конфігурація які фільтри показувати
```

**План налаштування:**
1. **Розкоментувати поля в Product:**
   - `brand` → ForeignKey(Brand)
   - `product_group` → ForeignKey(ProductGroup)
   - `purpose` → ForeignKey(ProductPurpose)

2. **Створити міграцію:**
   ```bash
   python manage.py makemigrations
   python manage.py migrate
   ```

3. **Налаштувати CategoryFilterConfig для кожної категорії:**
   - Вказати які фільтри показувати
   - Порядок відображення фільтрів

4. **Оновити views.py для підтримки фільтрації:**
   - Додати логіку фільтрації по підкатегоріях
   - Додати логіку фільтрації по брендам
   - Додати логіку фільтрації по групах
   - Додати логіку фільтрації по призначенню
   - Додати фільтр по ціні (min-max)
   - Додати фільтр по наявності

**Приклад структури фільтрів для категорії "Волосся":**
```
Фільтри:
│
├─ [Блок 1] Підкатегорії
│   ├─ Шампуні
│   ├─ Маски
│   ├─ Бальзами
│   └─ Сироватки
│
├─ [Блок 2] Бренд
│   ├─ Biki
│   ├─ Estel
│   └─ Kapous
│
├─ [Блок 3] Призначення
│   ├─ Для сухого волосся
│   ├─ Для жирного волосся
│   └─ Для фарбованого волосся
│
├─ [Блок 4] Ціна
│   └─ [слайдер] від 50₴ до 1000₴
│
└─ [Блок 5] Наявність
    └─ ☑ Тільки в наявності
```

**Результат:**
- Повний план структури фільтрів
- Схема UI для фільтрів
- Логіка роботи фільтрів (AND/OR)

---

### 2.4 Створення скриптів міграції
**Завдання:** Підготувати скрипти для автоматичної міграції даних

**Скрипт 1: Експорт даних зі старого сайту**
```
Файл: export_from_opencart.py
Функції:
- Підключення до БД OpenCart (MySQL)
- Експорт категорій → JSON
- Експорт товарів → JSON
- Експорт цін (tier prices, special prices) → JSON
- Експорт зображень (URL-и) → JSON
- Експорт характеристик → JSON
- Експорт фільтрів → JSON
```

**Скрипт 2: Імпорт категорій та підкатегорій**
```
Файл: apps/products/management/commands/import_categories.py
Функції:
- Читання JSON з категоріями
- Створення Category з правильною ієрархією
- Встановлення parent для підкатегорій
- Завантаження зображень категорій
- Створення CategoryFilterConfig для кожної
```

**Скрипт 3: Імпорт брендів, груп, призначень**
```
Файл: apps/products/management/commands/import_filters.py
Функції:
- Створення всіх Brand
- Створення всіх ProductGroup
- Створення всіх ProductPurpose
- Перевірка дублікатів
```

**Скрипт 4: Імпорт товарів**
```
Файл: apps/products/management/commands/import_products.py
Функції:
- Читання JSON з товарами
- Створення Product з усіма полями:
  * Базова інформація
  * Всі типи цін
  * Зв'язки з категоріями
  * Зв'язки з брендами/групами/призначеннями
  * Статуси (is_top, is_new, is_sale)
- Перевірка дублікатів за SKU
- Логування помилок
```

**Скрипт 5: Імпорт зображень**
```
Файл: apps/products/management/commands/import_images.py
Функції:
- Завантаження зображень зі старого сайту
- Оптимізація розміру (якщо потрібно)
- Завантаження на Cloudinary
- Створення ProductImage записів
- Встановлення is_main для головного фото
```

**Скрипт 6: Імпорт характеристик**
```
Файл: apps/products/management/commands/import_attributes.py
Функції:
- Створення ProductAttribute для кожного товару
- Групування характеристик
- Встановлення порядку відображення
```

**Скрипт 7: Перевірка цілісності**
```
Файл: apps/products/management/commands/check_migration.py
Функції:
- Порівняння кількості товарів (старий vs новий)
- Перевірка чи всі категорії мігровані
- Перевірка чи всі зображення завантажені
- Пошук товарів без категорій
- Пошук товарів без зображень
- Пошук дублікатів
- Генерація звіту
```

**Результат:**
- 7 готових скриптів міграції
- Документація по використанню
- План запуску (черговість скриптів)

---

## 🚀 ЕТАП 3: ВИКОНАННЯ МІГРАЦІЇ

### 3.1 Підготовка середовища
**Завдання:** Налаштувати все необхідне перед міграцією

**Що зробити:**
- [ ] Створити резервну копію поточної БД Django
- [ ] Отримати доступ до БД OpenCart (MySQL)
- [ ] Налаштувати Cloudinary (API ключі)
- [ ] Перевірити що всі залежності встановлені:
  ```bash
  pip install mysql-connector-python  # для підключення до OpenCart DB
  pip install cloudinary               # для завантаження зображень
  pip install requests                 # для скачування зображень
  pip install pillow                   # для обробки зображень
  ```
- [ ] Створити тестову БД для перевірки (опціонально)
- [ ] Підготувати логування (для відстеження помилок)

**Результат:**
- Готове середовище для міграції
- Чеклист перевірки готовності

---

### 3.2 Фаза 1: Міграція довідників
**Завдання:** Мігрувати всі довідники (категорії, бренди тощо)

**Порядок виконання:**

**Крок 1: Експорт даних зі старого сайту**
```bash
# Підключитись до БД OpenCart
# Експортувати:
- oc_category → categories.json
- oc_manufacturer → brands.json  
- oc_filter_group + oc_filter → filters.json
```

**Крок 2: Розкоментувати поля в models.py**
```python
# У файлі apps/products/models.py розкоментувати:
class Product:
    brand = models.ForeignKey(Brand, ...)
    product_group = models.ForeignKey(ProductGroup, ...)
    purpose = models.ForeignKey(ProductPurpose, ...)
```

**Крок 3: Створити міграцію**
```bash
python manage.py makemigrations products
python manage.py migrate products
```

**Крок 4: Імпортувати категорії**
```bash
python manage.py import_categories --file=categories.json --dry-run
# Перевірити вивід, якщо все ОК:
python manage.py import_categories --file=categories.json
```

**Крок 5: Імпортувати бренди/групи/призначення**
```bash
python manage.py import_filters --file=filters.json --dry-run
python manage.py import_filters --file=filters.json
```

**Перевірка:**
- [ ] Всі категорії створені
- [ ] Ієрархія категорій правильна (parent встановлені)
- [ ] CategoryFilterConfig створені для всіх категорій
- [ ] Бренди створені
- [ ] Групи товарів створені
- [ ] Призначення створені
- [ ] Немає дублікатів

**Результат:**
- Всі довідники мігровані
- Звіт про міграцію (скільки записів створено)

---

### 3.3 Фаза 2: Міграція товарів
**Завдання:** Мігрувати всі товари з усіма даними

**Крок 1: Експорт товарів**
```bash
# З БД OpenCart експортувати:
- oc_product → products.json
- oc_product_description → products_descriptions.json
- oc_product_special → products_special_prices.json
- oc_product_discount → products_tier_prices.json
```

**Крок 2: Об'єднати всі дані**
```python
# Скрипт merge_product_data.py
# Об'єднує всі файли в один products_full.json з такою структурою:
{
  "product_id": 123,
  "name": "Назва товару",
  "model": "BS00001",  # SKU
  "description": "Опис",
  "category_id": 45,
  "manufacturer_id": 12,  # Brand
  "price": "100.00",      # retail_price
  "special_prices": [
    {"date_start": "...", "date_end": "...", "price": "80.00"}  # sale_price
  ],
  "tier_prices": [
    {"quantity": 3, "price": "95.00"},  # price_3_qty
    {"quantity": 5, "price": "90.00"}   # price_5_qty
  ],
  "customer_group_prices": [
    {"group": "Wholesale", "price": "85.00"}  # wholesale_price
  ],
  "stock": 50,
  "status": 1,
  "images": ["image1.jpg", "image2.jpg"],
  "attributes": [
    {"name": "Об'єм", "value": "250 мл"}
  ]
}
```

**Крок 3: Імпортувати товари (тільки основна інформація)**
```bash
python manage.py import_products --file=products_full.json --skip-images --dry-run
# Перевірити вивід
python manage.py import_products --file=products_full.json --skip-images
```

**Крок 4: Перевірка після імпорту**
```bash
python manage.py check_migration --report=migration_report.txt
```

**Що перевіряти:**
- [ ] Кількість товарів збігається
- [ ] Всі товари мають категорії
- [ ] Всі ціни правильно встановлені:
  - retail_price заповнена
  - wholesale_price де потрібно
  - sale_price де є акції
  - price_3_qty та price_5_qty де є градація
- [ ] SKU унікальні та правильні
- [ ] Описи товарів не порожні
- [ ] Статуси (is_active, is_sale, is_top, is_new) правильні
- [ ] Немає дублікатів (за SKU та назвою)

**Критичні помилки для виправлення:**
- Товари без категорій
- Товари з retail_price = 0
- Дублікати SKU
- Відсутні обов'язкові поля

**Результат:**
- Всі товари мігровані
- Звіт про міграцію
- Список товарів з помилками (якщо є)

---

### 3.4 Фаза 3: Міграція зображень
**Завдання:** Завантажити всі зображення товарів

**Крок 1: Підготовка списку зображень**
```bash
# Створити файл images_to_download.json зі списком:
[
  {
    "product_sku": "BS00001",
    "images": [
      {
        "url": "https://beautyshop-ukrane.com.ua/image/cache/catalog/product1.jpg",
        "is_main": true
      },
      {
        "url": "https://beautyshop-ukrane.com.ua/image/cache/catalog/product1-2.jpg",
        "is_main": false
      }
    ]
  }
]
```

**Крок 2: Завантаження зображень**
```bash
python manage.py import_images --file=images_to_download.json --batch-size=10
```

**Процес завантаження:**
1. Скачати зображення зі старого сайту
2. Оптимізувати розмір (якщо > 1MB)
3. Завантажити на Cloudinary
4. Створити ProductImage запис
5. Встановити is_main=True для головного фото
6. Логування (успішні/помилки)

**Обробка помилок:**
- Якщо зображення не існує → логувати, пропустити
- Якщо помилка завантаження → логувати, спробувати пізніше
- Якщо товар не знайдений за SKU → логувати, пропустити

**Перевірка:**
- [ ] Всі товари мають хоча б одне зображення
- [ ] Головне зображення встановлене (is_main=True)
- [ ] Зображення відображаються коректно
- [ ] Cloudinary URLs працюють
- [ ] Немає битих посилань

**Результат:**
- Всі зображення завантажені
- Звіт про завантаження (успішні/помилки)
- Список товарів без зображень (якщо є)

---

### 3.5 Фаза 4: Міграція характеристик
**Завдання:** Додати всі характеристики товарів

**Крок 1: Експорт характеристик**
```bash
# З БД OpenCart:
- oc_product_attribute → attributes.json
```

**Крок 2: Імпорт характеристик**
```bash
python manage.py import_attributes --file=attributes.json
```

**Що перевіряти:**
- [ ] Всі характеристики додані
- [ ] Порядок відображення правильний (sort_order)
- [ ] Немає порожніх значень
- [ ] Форматування значень коректне

**Результат:**
- Всі характеристики мігровані
- Товари мають повну інформацію

---

### 3.6 Фаза 5: Налаштування фільтрів для категорій
**Завдання:** Налаштувати які фільтри показувати для кожної категорії

**Ручне налаштування через Django Admin:**

**Для кожної категорії:**
1. Відкрити категорію в адмін-панелі
2. В блоці "Конфігурація фільтрів" вказати:
   - [ ] Показувати фільтр Бренд (так/ні)
   - [ ] Показувати фільтр Група (так/ні)
   - [ ] Показувати фільтр Призначення (так/ні)
   - [ ] Показувати фільтр Ціна (так/ні)
   - [ ] Показувати фільтр Наявність (так/ні)

**Приклади налаштувань:**

**Категорія "Волосся":**
- Підкатегорії: Шампуні, Маски, Бальзами (відображати як перший блок)
- Бренд: Так
- Група: Ні (не потрібно, використовуються підкатегорії)
- Призначення: Так (Для сухого, Для жирного, тощо)
- Ціна: Так
- Наявність: Так

**Категорія "Нігті":**
- Підкатегорії: Гель-лаки, Бази, Топи
- Бренд: Так
- Група: Так (Однофазні, Трифазні)
- Призначення: Так
- Ціна: Так
- Наявність: Так

**Результат:**
- Всі категорії налаштовані
- Фільтри відображаються правильно на фронтенді

---

## 🎨 ЕТАП 4: НАЛАШТУВАННЯ ФРОНТЕНДУ

### 4.1 Оновлення views.py для фільтрації
**Завдання:** Додати логіку фільтрації з підкатегоріями

**Файл:** `apps/products/views.py`

**Що додати:**
```python
class CategoryView(ListView):
    def get_queryset(self):
        queryset = Product.objects.filter(is_active=True)
        
        # Фільтр по категорії або підкатегорії
        category_slug = self.kwargs.get('slug')
        self.category = get_object_or_404(Category, slug=category_slug)
        
        # Включити товари з підкатегорій
        subcategories = self.category.children.filter(is_active=True)
        categories_to_filter = [self.category.id] + list(subcategories.values_list('id', flat=True))
        queryset = queryset.filter(category_id__in=categories_to_filter)
        
        # Фільтр по підкатегоріях (GET параметр)
        selected_subcats = self.request.GET.getlist('subcategory')
        if selected_subcats:
            queryset = queryset.filter(category__slug__in=selected_subcats)
        
        # Фільтр по бренду
        brands = self.request.GET.getlist('brand')
        if brands:
            queryset = queryset.filter(brand__slug__in=brands)
        
        # Фільтр по призначенню
        purposes = self.request.GET.getlist('purpose')
        if purposes:
            queryset = queryset.filter(purpose__slug__in=purposes)
        
        # Фільтр по ціні
        min_price = self.request.GET.get('min_price')
        max_price = self.request.GET.get('max_price')
        if min_price:
            queryset = queryset.filter(retail_price__gte=min_price)
        if max_price:
            queryset = queryset.filter(retail_price__lte=max_price)
        
        # Фільтр по наявності
        in_stock = self.request.GET.get('in_stock')
        if in_stock:
            queryset = queryset.filter(stock__gt=0)
        
        return queryset
    
    def get_context_data(self, **kwargs):
        context = super().get_context_data(**kwargs)
        context['category'] = self.category
        
        # Підкатегорії для відображення
        context['subcategories'] = self.category.children.filter(is_active=True)
        
        # Бренди для фільтра (тільки ті що є в товарах категорії)
        context['available_brands'] = Brand.objects.filter(
            product__category=self.category
        ).distinct()
        
        # Призначення для фільтра
        context['available_purposes'] = ProductPurpose.objects.filter(
            product__category=self.category
        ).distinct()
        
        # Діапазон цін
        price_range = Product.objects.filter(
            category=self.category
        ).aggregate(
            min_price=Min('retail_price'),
            max_price=Max('retail_price')
        )
        context['price_range'] = price_range
        
        # Конфігурація фільтрів
        try:
            context['filter_config'] = self.category.filter_config
        except:
            context['filter_config'] = None
        
        return context
```

**Результат:**
- Views підтримує всі типи фільтрів
- Фільтрація працює коректно

---

### 4.2 Створення шаблону фільтрів
**Завдання:** Створити UI для фільтрів збоку сторінки категорії

**Файл:** `templates/products/includes/filters.html`

**Структура:**
```html
<div class="filters-sidebar">
    <!-- Блок 1: Підкатегорії -->
    {% if subcategories %}
    <div class="filter-block">
        <h3>Підкатегорії</h3>
        <div class="filter-options">
            {% for subcat in subcategories %}
            <label class="filter-checkbox">
                <input type="checkbox" name="subcategory" value="{{ subcat.slug }}"
                    {% if subcat.slug in selected_subcategories %}checked{% endif %}>
                <span>{{ subcat.name }}</span>
            </label>
            {% endfor %}
        </div>
    </div>
    {% endif %}
    
    <!-- Блок 2: Бренд -->
    {% if filter_config.show_brand_filter and available_brands %}
    <div class="filter-block">
        <h3>Бренд</h3>
        <div class="filter-options">
            {% for brand in available_brands %}
            <label class="filter-checkbox">
                <input type="checkbox" name="brand" value="{{ brand.slug }}">
                <span>{{ brand.name }}</span>
            </label>
            {% endfor %}
        </div>
    </div>
    {% endif %}
    
    <!-- Блок 3: Призначення -->
    {% if filter_config.show_purpose_filter and available_purposes %}
    <div class="filter-block">
        <h3>Призначення</h3>
        <div class="filter-options">
            {% for purpose in available_purposes %}
            <label class="filter-checkbox">
                <input type="checkbox" name="purpose" value="{{ purpose.slug }}">
                <span>{{ purpose.name }}</span>
            </label>
            {% endfor %}
        </div>
    </div>
    {% endif %}
    
    <!-- Блок 4: Ціна -->
    {% if filter_config.show_price_filter %}
    <div class="filter-block">
        <h3>Ціна</h3>
        <div class="filter-price">
            <input type="range" name="min_price" min="{{ price_range.min_price }}" max="{{ price_range.max_price }}">
            <input type="range" name="max_price" min="{{ price_range.min_price }}" max="{{ price_range.max_price }}">
            <div class="price-display">
                <span id="min-price-display"></span> - <span id="max-price-display"></span> ₴
            </div>
        </div>
    </div>
    {% endif %}
    
    <!-- Блок 5: Наявність -->
    {% if filter_config.show_availability_filter %}
    <div class="filter-block">
        <label class="filter-checkbox">
            <input type="checkbox" name="in_stock" value="1">
            <span>Тільки в наявності</span>
        </label>
    </div>
    {% endif %}
    
    <button type="button" id="apply-filters" class="btn btn-primary">Застосувати фільтри</button>
    <button type="button" id="reset-filters" class="btn btn-secondary">Скинути</button>
</div>
```

**Результат:**
- Шаблон фільтрів створений
- Всі блоки відображаються правильно

---

### 4.3 JavaScript для фільтрів
**Завдання:** Додати інтерактивність фільтрам

**Файл:** `static/js/filters.js`

**Функціонал:**
```javascript
// Зібрати всі вибрані фільтри
// Оновити URL з GET параметрами
// Виконати AJAX запит для оновлення списку товарів
// Оновити список товарів без перезавантаження сторінки
// Оновити лічильники (скільки товарів знайдено)
```

**Результат:**
- Фільтри працюють без перезавантаження
- UX плавний та швидкий

---

### 4.4 Стилізація фільтрів
**Завдання:** Оформити фільтри відповідно до дизайну сайту

**Файл:** `static/css/filters.css`

**Вимоги:**
- Адаптивність (мобільна версія)
- Згортання/розгортання блоків фільтрів
- Візуальний feedback при виборі
- Smooth переходи
- Відповідність загальному стилю сайту

**Результат:**
- Фільтри виглядають професійно
- Зручні для використання

---

## ✅ ЕТАП 5: ТЕСТУВАННЯ ТА ПЕРЕВІРКА

### 5.1 Перевірка товарів
**Завдання:** Перевірити що всі товари мігровані коректно

**Тест-кейси:**

**1. Кількість товарів:**
- [ ] Порівняти загальну кількість в OpenCart vs Django
- [ ] Різниця не більше 1-2% (допустимі видалені товари)

**2. Категорії:**
- [ ] Кожен товар належить до категорії
- [ ] Немає товарів в неіснуючих категоріях
- [ ] Підкатегорії працюють правильно

**3. Ціни:**
- [ ] retail_price заповнена для всіх товарів
- [ ] Ціни більше 0
- [ ] wholesale_price ≤ retail_price (де заповнена)
- [ ] sale_price < retail_price (де заповнена)
- [ ] price_3_qty ≤ retail_price (де заповнена)
- [ ] price_5_qty ≤ price_3_qty (де заповнена)
- [ ] Градація цін логічна: 5+ ≤ 3+ ≤ Опт ≤ Роздріб

**4. Зображення:**
- [ ] Кожен товар має хоча б одне зображення
- [ ] Головне зображення встановлене
- [ ] Зображення відкриваються
- [ ] Немає битих посилань

**5. Характеристики:**
- [ ] Товари мають характеристики (де вони були)
- [ ] Характеристики відображаються коректно

**6. Статуси:**
- [ ] is_active встановлений правильно
- [ ] is_sale відповідає наявності sale_price
- [ ] Бейджі відображаються (Хіт, Новинка, Акція)

**Результат:**
- Всі тести пройдені
- Товари мігровані повністю

---

### 5.2 Перевірка фільтрів
**Завдання:** Перевірити роботу системи фільтрації

**Тест-кейси:**

**1. Фільтр по підкатегоріях:**
- [ ] Підкатегорії відображаються в першому блоці
- [ ] Можна вибрати одну підкатегорію
- [ ] Можна вибрати кілька підкатегорій (OR логіка)
- [ ] Товари фільтруються правильно
- [ ] Лічильник показує правильну кількість

**2. Фільтр по бренду:**
- [ ] Всі бренди з товарами відображаються
- [ ] Можна вибрати кілька брендів
- [ ] Фільтрація працює правильно

**3. Фільтр по призначенню:**
- [ ] Всі призначення відображаються
- [ ] Фільтрація працює

**4. Фільтр по ціні:**
- [ ] Слайдер працює
- [ ] Min/max ціни правильні
- [ ] Фільтрація по діапазону працює

**5. Фільтр по наявності:**
- [ ] Показуються тільки товари в наявності (stock > 0)

**6. Комбінація фільтрів:**
- [ ] Можна комбінувати різні фільтри
- [ ] Логіка AND між різними типами фільтрів
- [ ] Логіка OR всередині одного типу (наприклад, кілька брендів)

**Результат:**
- Фільтри працюють бездоганно
- UX інтуїтивний

---

### 5.3 Перевірка цінової системи
**Завдання:** Перевірити що ціни відображаються правильно

**Тест-кейси:**

**1. Роздрібний клієнт (не зареєстрований):**
- [ ] Бачить retail_price
- [ ] Бачить sale_price (якщо є)
- [ ] Бачить градацію цін (від 3шт, від 5шт)
- [ ] Ціна автоматично змінюється при зміні кількості в кошику

**2. Оптовий клієнт (зареєстрований):**
- [ ] Бачить wholesale_price (якщо зареєстрований як оптовий)
- [ ] Бачить градацію цін
- [ ] Ціна враховує найвигідніший варіант

**3. Акційна ціна:**
- [ ] Відображається якщо is_sale=True
- [ ] Враховується період акції (start_date, end_date)
- [ ] Бейдж "Акція" відображається
- [ ] Стара ціна перекреслена, нова ціна виділена

**4. Градація цін в кошику:**
- [ ] При додаванні 3 шт → ціна змінюється на price_3_qty
- [ ] При додаванні 5 шт → ціна змінюється на price_5_qty
- [ ] Загальна сума розраховується правильно

**Результат:**
- Цінова система працює коректно
- Ціни відображаються правильно для всіх типів користувачів

---

### 5.4 Перевірка на дублікати
**Завдання:** Переконатись що немає дублікатів товарів

**Перевірки:**
```bash
python manage.py check_migration --check-duplicates
```

**Що перевіряти:**
- [ ] Дублікати за SKU
- [ ] Дублікати за назвою та категорією
- [ ] Дублікати за slug

**Дії при знаходженні дублікатів:**
1. Визначити який товар оригінальний
2. Об'єднати дані (якщо потрібно)
3. Видалити дублікат
4. Оновити посилання (якщо є)

**Результат:**
- Дублікатів немає
- База даних чиста

---

### 5.5 Перевірка SEO
**Завдання:** Перевірити що всі SEO поля заповнені

**Перевірки:**
- [ ] Всі категорії мають meta_title
- [ ] Всі категорії мають meta_description
- [ ] Всі товари мають slug
- [ ] Slugs унікальні
- [ ] URLs працюють правильно

**Результат:**
- SEO налаштоване
- Сайт готовий до індексації

---

## 📊 ЕТАП 6: ФІНАЛЬНА ПЕРЕВІРКА ТА ЗВІТ

### 6.1 Генерація підсумкового звіту
**Завдання:** Створити детальний звіт про міграцію

**Команда:**
```bash
python manage.py generate_migration_report --output=MIGRATION_REPORT.txt
```

**Структура звіту:**
```
==================================================
  ЗВІТ ПРО МІГРАЦІЮ BEAUTYSHOP
  Дата: 06.11.2025
==================================================

1. СТАТИСТИКА

Категорії:
- Всього мігровано: 25
- З них підкатегорій: 87
- Активних: 23
- Неактивних: 2

Товари:
- На старому сайті: 1523
- Мігровано: 1520
- Не мігровано: 3 (причини в додатку A)
- З дублікатами: 0

Зображення:
- Всього зображень: 4567
- Завантажено: 4550
- Помилки завантаження: 17 (список в додатку B)

Характеристики:
- Всього записів: 12450
- Мігровано: 12450

Фільтри:
- Бренди: 45
- Групи товарів: 23
- Призначення: 67

2. ЦІНИ

Товари з retail_price: 1520 (100%)
Товари з wholesale_price: 856 (56.3%)
Товари з sale_price: 234 (15.4%)
Товари з price_3_qty: 1203 (79.1%)
Товари з price_5_qty: 1198 (78.8%)

Перевірка логіки цін: ✅ Пройдено

3. ФІЛЬТРИ

Категорії з налаштованими фільтрами: 25/25 (100%)

4. ПРОБЛЕМИ ТА ПОПЕРЕДЖЕННЯ

⚠️ Товари без зображень: 3 (див. додаток C)
⚠️ Категорії без товарів: 1 (категорія "Тест" - можна видалити)
✅ Дублікати: не знайдено

5. РЕКОМЕНДАЦІЇ

- Перевірити та додати зображення для 3 товарів
- Видалити порожню категорію "Тест"
- Налаштувати оптові ціни для 664 товарів
- Провести SEO аудит після запуску

==================================================
```

**Додатки до звіту:**
- **Додаток A:** Список не мігрованих товарів з причинами
- **Додаток B:** Список помилок завантаження зображень
- **Додаток C:** Товари без зображень
- **Додаток D:** Порівняльна таблиця OpenCart vs Django

**Результат:**
- Повний звіт про міграцію
- Зрозуміла картина стану проекту

---

### 6.2 Фінальний чек-лист
**Завдання:** Перевірити що все готово до запуску

**Критичні пункти:**
- [ ] Всі категорії мігровані та активні
- [ ] Всі підкатегорії працюють
- [ ] Товари повністю мігровані (>99%)
- [ ] Зображення завантажені
- [ ] Характеристики додані
- [ ] Фільтри налаштовані для всіх категорій
- [ ] Фільтри працюють на фронтенді
- [ ] Ціни відображаються правильно
- [ ] Градація цін працює
- [ ] Оптові ціни працюють
- [ ] Акційні ціни працюють з датами
- [ ] Бейджі відображаються (Хіт, Новинка, Акція)
- [ ] Кошик працює з новими цінами
- [ ] SEO налаштоване
- [ ] Немає дублікатів
- [ ] Немає битих посилань
- [ ] Мобільна версія працює
- [ ] Фільтри працюють на мобільних
- [ ] Швидкість завантаження прийнятна
- [ ] Backup БД створений

**Результат:**
- Всі пункти виконані ✅
- Сайт готовий до запуску

---

## 🚨 ЕТАП 7: ВИПРАВЛЕННЯ ПОМИЛОК ТА ОПТИМІЗАЦІЯ

### 7.1 Робота з виявленими проблемами
**Завдання:** Виправити всі критичні та важливі помилки

**Пріоритизація проблем:**

**Критичні (P0) - виправити перед запуском:**
- Товари без категорій
- Товари з ціною 0
- Биті посилання на зображення
- Дублікати товарів

**Важливі (P1) - виправити протягом тижня:**
- Товари без зображень
- Товари без характеристик
- Неповні описи товарів
- Відсутні оптові ціни де потрібно

**Бажані (P2) - виправити протягом місяця:**
- SEO оптимізація описів
- Додаткові зображення
- Покращення характеристик
- Оптимізація фільтрів

**Процес виправлення:**
1. Створити список проблем з пріоритетами
2. Виправити критичні
3. Перевірити виправлення
4. Виправити важливі
5. Запланувати бажані

**Результат:**
- Всі критичні проблеми виправлені
- План роботи з іншими проблемами

---

### 7.2 Оптимізація продуктивності
**Завдання:** Переконатись що сайт працює швидко

**Що оптимізувати:**

**1. Запити до БД:**
```python
# В views.py додати:
.select_related('category', 'brand')
.prefetch_related('images', 'attributes')
```

**2. Кешування:**
```python
# Кешувати список категорій
# Кешувати фільтри для категорій
# Кешувати діапазон цін
```

**3. Cloudinary оптимізація:**
```python
# Використовувати трансформації Cloudinary
# Різні розміри для різних девайсів
# WebP формат де підтримується
# Lazy loading для зображень
```

**4. Індекси БД:**
```python
# Додати індекси для:
- category_id + is_active
- brand_id
- is_sale + is_active
- price fields для сортування
```

**Результат:**
- Сторінки завантажуються швидко (< 2 сек)
- Фільтри працюють миттєво (< 500мс)
- Сайт відповідає вимогам Core Web Vitals

---

### 7.3 Налаштування моніторингу
**Завдання:** Налаштувати відстеження після запуску

**Що моніторити:**
- [ ] Час відповіді сторінок
- [ ] Помилки 404 (биті посилання)
- [ ] Помилки 500 (серверні помилки)
- [ ] Кількість використання фільтрів
- [ ] Популярні товари/категорії
- [ ] Конверсія (додавання в кошик)

**Інструменти:**
- Django Debug Toolbar (тільки на dev)
- Sentry для моніторингу помилок
- Google Analytics для аналітики
- Custom логування в БД

**Результат:**
- Система моніторингу працює
- Помилки відстежуються

---

## 📚 ДОДАТКИ

### Додаток A: Структура БД OpenCart (основні таблиці)

```
oc_category
- category_id (PK)
- parent_id (FK)
- sort_order
- status

oc_category_description
- category_id (FK)
- name
- description
- meta_title
- meta_description

oc_product
- product_id (PK)
- model (SKU)
- quantity (stock)
- price (retail_price)
- status
- sort_order
- manufacturer_id (brand)

oc_product_description
- product_id (FK)
- name
- description
- meta_title
- meta_description

oc_product_to_category
- product_id (FK)
- category_id (FK)

oc_product_special
- product_id (FK)
- price (sale_price)
- date_start
- date_end
- customer_group_id

oc_product_discount
- product_id (FK)
- quantity (tier quantity)
- price (tier price)
- customer_group_id

oc_product_image
- product_id (FK)
- image (path)
- sort_order

oc_product_attribute
- product_id (FK)
- attribute_id (FK)
- text (value)

oc_manufacturer
- manufacturer_id (PK)
- name (Brand name)

oc_filter_group
- filter_group_id (PK)
- name (Filter group name)

oc_filter
- filter_id (PK)
- filter_group_id (FK)
- name (Filter value)
```

---

### Додаток B: Відповідність полів OpenCart → Django

| OpenCart | Django Product | Тип | Примітка |
|----------|---------------|-----|----------|
| `product.model` | `sku` | CharField | Артикул |
| `product_description.name` | `name` | CharField | Назва товару |
| `product_description.description` | `description` | TextField | Опис |
| `product.price` | `retail_price` | DecimalField | Базова ціна |
| `product_special.price` | `sale_price` | DecimalField | Акційна ціна |
| `product_discount (qty=3)` | `price_3_qty` | DecimalField | Ціна від 3шт |
| `product_discount (qty=5)` | `price_5_qty` | DecimalField | Ціна від 5шт |
| `product_special (group=wholesale)` | `wholesale_price` | DecimalField | Оптова ціна |
| `product.quantity` | `stock` | Integer | Кількість |
| `product.status` | `is_active` | Boolean | Статус |
| `product_to_category.category_id` | `category` | ForeignKey | Категорія |
| `product.manufacturer_id` | `brand` | ForeignKey | Бренд |
| `product_image.image` | `ProductImage` | Model | Зображення |
| `product_attribute` | `ProductAttribute` | Model | Характеристики |
| `product_description.meta_title` | `meta_title` | CharField | SEO |
| `product_description.meta_description` | `meta_description` | TextField | SEO |
| `category.parent_id` | `Category.parent` | ForeignKey | Підкатегорія |

---

### Додаток C: Чек-лист для кожного товару

При перевірці товару переконатись що:
- [ ] Назва заповнена та коректна
- [ ] SKU унікальний та правильний формат (BS##### )
- [ ] Категорія вказана та існує
- [ ] retail_price > 0
- [ ] Якщо є wholesale_price → <= retail_price
- [ ] Якщо є sale_price → < retail_price
- [ ] Якщо є price_3_qty → <= retail_price
- [ ] Якщо є price_5_qty → <= price_3_qty
- [ ] stock >= 0
- [ ] is_active відповідає статусу на старому сайті
- [ ] Хоча б одне зображення
- [ ] Головне зображення встановлене (is_main=True)
- [ ] Опис не порожній та інформативний
- [ ] Характеристики додані (якщо були)
- [ ] Slug згенерований правильно
- [ ] Немає дублікатів за SKU
- [ ] Бренд вказаний (якщо можливо)

---

### Додаток D: Команди для перевірки

**Перевірка категорій:**
```bash
python manage.py shell
>>> from apps.products.models import Category
>>> Category.objects.count()  # Кількість категорій
>>> Category.objects.filter(parent__isnull=True).count()  # Головні категорії
>>> Category.objects.filter(parent__isnull=False).count()  # Підкатегорії
```

**Перевірка товарів:**
```bash
>>> from apps.products.models import Product
>>> Product.objects.count()  # Всього товарів
>>> Product.objects.filter(retail_price=0).count()  # Товари з ціною 0
>>> Product.objects.filter(images__isnull=True).count()  # Товари без фото
>>> Product.objects.filter(category__isnull=True).count()  # Товари без категорії
```

**Перевірка цін:**
```bash
>>> Product.objects.filter(wholesale_price__gt=models.F('retail_price')).count()  # Оптова > роздрібної
>>> Product.objects.filter(sale_price__gt=models.F('retail_price')).count()  # Акція > базової
>>> Product.objects.filter(price_5_qty__gt=models.F('price_3_qty')).count()  # 5шт > 3шт
```

**Перевірка дублікатів:**
```bash
>>> from django.db.models import Count
>>> Product.objects.values('sku').annotate(count=Count('id')).filter(count__gt=1)  # Дублікати SKU
```

---

## 🎯 ПІДСУМОК

### Критичний шлях (послідовність дій):

1. **Тиждень 1: Аналіз (Етап 1)**
   - День 1-2: Аналіз категорій та підкатегорій
   - День 3-4: Аналіз товарів та цін
   - День 5: Аналіз фільтрів та характеристик

2. **Тиждень 2: Підготовка (Етап 2)**
   - День 1-2: Доопрацювання моделей Django
   - День 3-5: Створення скриптів міграції

3. **Тиждень 3: Міграція довідників (Етап 3.1-3.2)**
   - День 1-2: Міграція категорій та підкатегорій
   - День 3-4: Міграція брендів, груп, призначень
   - День 5: Перевірка та виправлення

4. **Тиждень 4: Міграція товарів (Етап 3.3-3.4)**
   - День 1-3: Міграція товарів з усіма даними
   - День 4-5: Міграція зображень

5. **Тиждень 5: Характеристики та фільтри (Етап 3.5-3.6)**
   - День 1-2: Міграція характеристик
   - День 3-4: Налаштування фільтрів для категорій
   - День 5: Перевірка

6. **Тиждень 6: Фронтенд (Етап 4)**
   - День 1-2: Views та логіка фільтрації
   - День 3-4: UI фільтрів
   - День 5: JavaScript та тестування

7. **Тиждень 7: Тестування (Етап 5)**
   - День 1-2: Функціональне тестування
   - День 3-4: Виправлення помилок
   - День 5: Фінальна перевірка

8. **Тиждень 8: Запуск (Етап 6-7)**
   - День 1-2: Генерація звітів та аналіз
   - День 3-4: Виправлення критичних помилок
   - День 5: Оптимізація та запуск

### Очікувані результати:

✅ **Категорії та підкатегорії:**
- Повна ієрархія збережена
- Фільтри працюють по підкатегоріях

✅ **Товари:**
- 100% товарів мігровані (допустима похибка <1%)
- Вся інформація збережена

✅ **Ціни:**
- Всі типи цін працюють
- Градація цін відображається правильно
- Оптові ціни для зареєстрованих

✅ **Фільтри:**
- Повна система фільтрації
- Інтуїтивний UI
- Швидка робота

✅ **Зображення:**
- Всі фото завантажені на Cloudinary
- Оптимізовані для швидкого завантаження

✅ **Якість:**
- Немає дублікатів
- Немає битих посилань
- SEO налаштоване
- Код чистий та задокументований

---

**КІНЕЦЬ ПЛАНУ**

**Дата створення:** 06.11.2025  
**Автор:** AI Assistant  
**Версія:** 1.0

---

## 📝 ПРИМІТКИ

- Цей план є робочим документом і може оновлюватись по ходу роботи
- Всі терміни є орієнтовними та можуть змінюватись
- Критичні помилки потрібно виправляти негайно
- Після кожного етапу робити backup БД
- Регулярно комітити зміни в Git
- Документувати всі важливі рішення

**Успіхів у міграції! 🚀**

